fn to_array(x: u64) -> [u64; 6]{
    let preformat: &str = x.parse::<u64>();
    let mut out: [u64; 6];

    let mut position = 0;
    for i in 
}

pub fn puzzle04(bound_high: u64, bound_low: u64){
    let result: Vec<u64> = vec![0;6];

    
}